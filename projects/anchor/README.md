# Anchor

Anchor is a web landing pages builder. Anchor is built with React and uses Vike to manage different rendering types (SSR and SSG) with a file system and multiple custom hooks to help with content and content's languages management.