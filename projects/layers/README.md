# Layers

Layers is a React web application UI builder. It provides themes, modals, panels, drawrs, form, form controls and a notifications system and manages the state through multiple context providers.