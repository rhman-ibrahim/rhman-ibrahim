# Authen

Authen is an authentication management application. Authen uses **JWT** to provide a stateless authentication its back-end is built with Python's **Django** and Django Rest Framework (**DRF**) to provide the API layer and it comes with a React front-end.